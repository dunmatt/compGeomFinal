// Generated by CoffeeScript 1.4.0
(function() {
  var mousedown, points, svg;

  points = [];

  mousedown = function() {
    var point;
    point = d3.mouse(this);
    points[points.length] = {
      x: point[0],
      y: point[1]
    };
    return svg.selectAll("circle").data(points).enter().append("circle").attr("cx", function(n) {
      return n.x;
    }).attr("cy", function(n) {
      return n.y;
    }).attr("r", "5").call(d3.behavior.drag());
  };

  svg = d3.select("body").append("svg").attr("width", 700).attr("height", 400).on("mousedown", mousedown);

}).call(this);
